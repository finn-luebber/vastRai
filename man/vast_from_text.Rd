% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vast_from_text.R
\name{vast_from_text}
\alias{vast_from_text}
\title{Translate Theory Text to VAST Models (Full Workflow)}
\usage{
vast_from_text(theory, context = NULL, analyst = NULL, output_file = NULL)
}
\arguments{
\item{theory}{Character string. The theory text to translate. See
\code{\link[=narrative_to_prompt]{narrative_to_prompt()}} for guidance on what to include.}

\item{context}{Character string or NULL. Optional additional context.}

\item{analyst}{Character string or NULL. Name of the analyst to record in
the VAST models. Defaults to NULL (LLM identifies itself). See
\code{\link[=narrative_to_prompt]{narrative_to_prompt()}} for details.}

\item{output_file}{Character string or NULL. If provided, also writes the
prompt to this file.}
}
\value{
Invisibly returns the prompt string. The function's main purpose is
its side effects: printing the prompt and workflow instructions.
}
\description{
This is the primary user-facing function. It generates a prompt, displays
workflow instructions, and — once the user provides JSON from the LLM —
builds the VAST models.
}
\details{
This function is a convenience wrapper around \code{\link[=narrative_to_prompt]{narrative_to_prompt()}}. The
complete workflow is:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Step 1: Generate the prompt
vast_from_text("Your theory text here...")

# Step 2: Paste prompt into LLM, get JSON response, save to file

# Step 3: Build models from JSON
result <- vast_from_json("response.json")

# Step 4: Explore
vastR::vast_render(result$closest_match)
vastR::vast_render(result$distant_compatible)
print_translation_notes(result)
print_rigor_assessment(result)
cat(result$r_code$closest_match)
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
vast_from_text(
  "Smoking causes tar buildup in the lungs, which in turn
   causes lung cancer."
)

# With user as analyst from the start:
vast_from_text(
  "Smoking causes tar buildup in the lungs, which in turn
   causes lung cancer.",
  analyst = "Finn"
)
}

}
\seealso{
\code{\link[=narrative_to_prompt]{narrative_to_prompt()}}, \code{\link[=vast_from_json]{vast_from_json()}}
}
