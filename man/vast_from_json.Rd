% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vast_from_json.R
\name{vast_from_json}
\alias{vast_from_json}
\title{Build VAST Models from LLM JSON Output}
\usage{
vast_from_json(json_input, which = "both", render = FALSE)
}
\arguments{
\item{json_input}{Either a file path to a JSON file, or a JSON string.}

\item{which}{Character. Which model(s) to build: \code{"both"} (default),
\code{"closest"}, or \code{"distant"}.}

\item{render}{Logical. If \code{TRUE}, renders the model(s) immediately via
\code{vastR::vast_render()}. Default is \code{FALSE}.}
}
\value{
A list of class \code{vast_translation} with components:
\describe{
\item{closest_match}{A vastR model object (if requested).}
\item{distant_compatible}{A vastR model object (if requested).}
\item{translation_notes}{A list containing auxiliary assumptions,
underspecification points, and the model divergence summary.}
\item{rigor_assessment}{A list containing the T-item ratings and
overall assessment.}
\item{source_text}{The original narrative paragraph.}
\item{r_code}{A list with \code{closest_match} and/or \code{distant_compatible}
entries, each containing the R code string that reproduces the model.}
}
}
\description{
Parses the JSON output from an LLM (generated via a \code{\link[=narrative_to_prompt]{narrative_to_prompt()}}
prompt) and constructs one or two vastR model objects, along with
translation notes and a rigor assessment.
}
\details{
The function maps JSON node/edge specifications to the exact vastR
constructors:\tabular{ll}{
   JSON type \tab vastR function \cr
   concept \tab \code{\link[vastR:vast_concept]{vastR::vast_concept()}} \cr
   name \tab \code{\link[vastR:vast_name]{vastR::vast_name()}} \cr
   data \tab \code{\link[vastR:vast_data]{vastR::vast_data()}} \cr
   is \tab \code{\link[vastR:vast_is]{vastR::vast_is()}} \cr
   ought \tab \code{\link[vastR:vast_ought]{vastR::vast_ought()}} \cr
   perspective \tab \code{\link[vastR:vast_perspective]{vastR::vast_perspective()}} \cr
   diamond \tab \code{\link[vastR:vast_diamond]{vastR::vast_diamond()}} \cr
   noise_source \tab \code{\link[vastR:vast_noise_source]{vastR::vast_noise_source()}} \cr
}


For edges:\tabular{ll}{
   JSON type \tab vastR function \cr
   naming \tab \code{\link[vastR:vast_naming]{vastR::vast_naming()}} \cr
   implication \tab \code{\link[vastR:vast_implication]{vastR::vast_implication()}} \cr
   causation \tab \code{\link[vastR:vast_causation]{vastR::vast_causation()}} \cr
   transformation \tab \code{\link[vastR:vast_transformation]{vastR::vast_transformation()}} \cr
   prediction \tab \code{\link[vastR:vast_prediction]{vastR::vast_prediction()}} \cr
   reasoning \tab \code{\link[vastR:vast_reasoning]{vastR::vast_reasoning()}} \cr
   unknown \tab \code{\link[vastR:vast_unknown]{vastR::vast_unknown()}} \cr
   noise \tab \code{\link[vastR:vast_noise]{vastR::vast_noise()}} \cr
   structural \tab \code{\link[vastR:vast_relation]{vastR::vast_relation()}} with \code{type = ""} \cr
}
}
\examples{
\dontrun{
result <- vast_from_json("llm_response.json")
vastR::vast_render(result$closest_match)
cat(result$r_code$closest_match)
print_translation_notes(result)
}

}
\seealso{
\code{\link[=narrative_to_prompt]{narrative_to_prompt()}}, \code{\link[=print_translation_notes]{print_translation_notes()}},
\code{\link[=print_rigor_assessment]{print_rigor_assessment()}}
}
